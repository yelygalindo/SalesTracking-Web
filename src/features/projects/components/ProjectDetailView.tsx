/* eslint-disable @typescript-eslint/no-unused-expressions */
import { useMemo, useState, type FormEvent } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { BellPlus, CheckCircle2, FileText, MapPin, Package, Pencil, Upload, X } from 'lucide-react'
import { LocationViewer } from '@/components/maps/LocationViewer'
import { ConfirmDialog } from '@/components/feedback/ConfirmDialog'
import { notify } from '@/components/feedback/toast'
import type { ProjectDetail, ProjectStatus, TimelineItem } from '../api/projectDtos'
import { projectService } from '../services/projectService'
import { formatBytes, validateAttachment } from '../services/attachmentValidation'

type MainTab='summary'|'activity'|'attachments'|'products'
type ActivityFilter='all'|'visits'|'notes'|'reminders'|'files'
type FileFilter='all'|'photos'|'documents'

export function ProjectDetailView({project,statuses,canUpdate,canDelete,canStatus,onEdit,onDelete,onStatusChange}:{project:ProjectDetail;statuses:ProjectStatus[];canUpdate:boolean;canDelete:boolean;canStatus:boolean;onEdit:()=>void;onDelete:()=>void;onStatusChange:(id:number)=>void}){
 const[tab,setTab]=useState<MainTab>('summary'),[changingStatus,setChangingStatus]=useState(false)
 return <div className="project-detail-page"><header className="project-detail-hero"><div><p className="overline">Proyecto</p><div className="project-title-row"><h2>{project.name}</h2><span className="status-pill">{project.status}</span></div><p>{project.customerName||'Sin cliente'} · {project.sellerName||'Sin responsable'}</p></div><div className="project-header-actions">{canStatus&&!changingStatus&&<button className="secondary-action" onClick={()=>setChangingStatus(true)}>Cambiar estado</button>}{changingStatus&&<label className="inline-status">Estado<select autoFocus value={statuses.find(item=>item.label.toLowerCase()===project.status.toLowerCase())?.value||''} onChange={event=>{onStatusChange(Number(event.target.value));setChangingStatus(false)}}>{statuses.map(item=><option key={item.value} value={item.value}>{item.label}</option>)}</select><button onClick={()=>setChangingStatus(false)} aria-label="Cancelar cambio de estado"><X/></button></label>}{canUpdate&&<button className="action-primary" onClick={onEdit}><Pencil/>Editar</button>}{canDelete&&<button className="danger-button" onClick={onDelete}>Eliminar</button>}</div></header><nav className="project-detail-tabs">{([['summary','Resumen'],['activity','Actividad'],['attachments','Adjuntos'],['products','Productos']]as[MainTab,string][]).map(([value,label])=><button className={tab===value?'active':''} key={value} onClick={()=>setTab(value)}>{label}</button>)}</nav>{tab==='summary'&&<ProjectSummary project={project}/>} {tab==='activity'&&<ProjectActivity id={project.externalId} canUpdate={canUpdate}/>} {tab==='attachments'&&<ProjectAttachments id={project.externalId} editable={canUpdate}/>} {tab==='products'&&<ProjectProducts id={project.externalId}/>}</div>
}

function ProjectSummary({project}:{project:ProjectDetail}){const money=project.estimatedAmount==null?'—':new Intl.NumberFormat('es-BO',{style:'currency',currency:'BOB'}).format(project.estimatedAmount);return <div className="project-summary"><section className="project-facts"><h3>Información general</h3><dl><div><dt>Cliente</dt><dd>{project.customerName||'Sin cliente'}</dd></div><div><dt>Responsable</dt><dd>{project.sellerName||'Sin responsable'}</dd></div><div><dt>Estado</dt><dd><span className="status-pill">{project.status}</span></dd></div><div><dt>Monto estimado</dt><dd>{money}</dd></div>{project.startDateUtc&&<div><dt>Fecha de inicio</dt><dd>{formatDate(project.startDateUtc)}</dd></div>}{project.expectedCloseDateUtc&&<div><dt>Cierre estimado</dt><dd>{formatDate(project.expectedCloseDateUtc)}</dd></div>}{project.actualCloseDateUtc&&<div><dt>Cierre real</dt><dd>{formatDate(project.actualCloseDateUtc)}</dd></div>}</dl></section><section className="project-progress-card"><header><h3>Avance del proyecto</h3><strong>{project.progressPercentage}%</strong></header><progress max="100" value={project.progressPercentage}/></section>{project.description&&<section className="project-description"><h3>Descripción</h3><p>{project.description}</p></section>}{(project.address||project.latitude!=null)&&<section className="project-location"><h3>Ubicación</h3><LocationViewer latitude={project.latitude} longitude={project.longitude} label={project.address}/></section>}</div>}

const activityInfo=(item:TimelineItem)=>{const value=`${item.eventTypeName} ${item.title}`.toLowerCase();if(value.includes('check-in')||value.includes('checkin'))return{label:'Visita iniciada',kind:'visits' as const,Icon:MapPin,tone:'orange'};if(value.includes('check-out')||value.includes('checkout'))return{label:'Visita finalizada',kind:'visits' as const,Icon:CheckCircle2,tone:'green'};if(value.includes('note'))return{label:'Nota agregada',kind:'notes' as const,Icon:Pencil,tone:'blue'};if(value.includes('attachment')||value.includes('archivo'))return{label:'Archivo agregado',kind:'files' as const,Icon:FileText,tone:'blue'};if(value.includes('reminder')&&value.includes('complet'))return{label:'Recordatorio completado',kind:'reminders' as const,Icon:CheckCircle2,tone:'green'};if(value.includes('reminder'))return{label:'Recordatorio creado',kind:'reminders' as const,Icon:BellPlus,tone:'orange'};return{label:item.title||'Proyecto actualizado',kind:'all' as const,Icon:Pencil,tone:'blue'}}
function ProjectActivity({id,canUpdate}:{id:string;canUpdate:boolean}){const cache=useQueryClient(),[filter,setFilter]=useState<ActivityFilter>('all'),[action,setAction]=useState<'note'|'reminder'|null>(null),[text,setText]=useState(''),[date,setDate]=useState('');const timeline=useQuery({queryKey:['project-timeline',id],queryFn:()=>projectService.timeline(id)});const addNote=useMutation({mutationFn:()=>projectService.addNote(id,text),onSuccess:async()=>{setText('');setAction(null);await cache.invalidateQueries({queryKey:['project-timeline',id]})}}),addReminder=useMutation({mutationFn:()=>projectService.addReminder(id,text,new Date(date).toISOString()),onSuccess:async()=>{setText('');setDate('');setAction(null);await cache.invalidateQueries({queryKey:['project-timeline',id]})}});const items=(timeline.data?.items||[]).filter(item=>filter==='all'||activityInfo(item).kind===filter);return <section className="project-activity"><header><nav>{([['all','Todos'],['visits','Visitas'],['notes','Notas'],['reminders','Recordatorios'],['files','Archivos']]as[ActivityFilter,string][]).map(([value,label])=><button className={filter===value?'active':''} key={value} onClick={()=>setFilter(value)}>{label}</button>)}</nav>{canUpdate&&<div><button onClick={()=>setAction('note')}>+ Nueva nota</button><button onClick={()=>setAction('reminder')}>+ Recordatorio</button></div>}</header>{action&&<form className="project-quick-form" onSubmit={event=>{event.preventDefault();if(!text.trim())return;action==='note'?addNote.mutate():date&&addReminder.mutate()}}><input autoFocus value={text} onChange={event=>setText(event.target.value)} placeholder={action==='note'?'Escribe una nota':'Descripción del recordatorio'}/>{action==='reminder'&&<input required type="datetime-local" value={date} onChange={event=>setDate(event.target.value)}/>}<button className="action-primary">Guardar</button><button type="button" onClick={()=>setAction(null)}>Cancelar</button></form>}<div className="project-timeline">{items.map(item=>{const info=activityInfo(item);return <article key={item.externalId}><span className={`event-icon ${info.tone}`}><info.Icon/></span><div><strong>{info.label}</strong>{item.description&&<p>{item.description}</p>}<small>{item.createdBy?.name||'Sistema'} · <time>{formatDateTime(item.occurredAtUtc)}</time></small></div></article>})}{!timeline.isLoading&&!items.length&&<p className="activity-empty">No hay actividad para este filtro.</p>}</div></section>}

function ProjectProducts({id}:{id:string}){const query=useQuery({queryKey:['project-materials',id],queryFn:()=>projectService.materials(id)});return <section className="project-products">{query.data?.map(item=><article key={item.productExternalId}><Package/><div><strong>{item.productName}</strong><p>Comprometido: {item.committedQuantity} {item.unit} · Entregado: {item.deliveredQuantity} · Pendiente: {item.pendingQuantity}</p></div></article>)}{!query.isLoading&&!query.data?.length&&<p className="activity-empty">Sin productos asociados a entregas.</p>}</section>}

function ProjectAttachments({id,editable}:{id:string;editable:boolean}){const cache=useQueryClient(),query=useQuery({queryKey:['project-attachments',id],queryFn:()=>projectService.attachments(id)}),options=useQuery({queryKey:['project-attachment-options'],queryFn:projectService.attachmentOptions}),visits=useQuery({queryKey:['project-visits',id],queryFn:()=>projectService.visits(id)});const[filter,setFilter]=useState<FileFilter>('all'),[uploadOpen,setUploadOpen]=useState(false),[visit,setVisit]=useState(''),[type,setType]=useState(''),[caption,setCaption]=useState(''),[isCover,setIsCover]=useState(false),[file,setFile]=useState<File|null>(null),[validation,setValidation]=useState<string|null>(null),[deleting,setDeleting]=useState<{id:string;name:string}|null>(null);const refresh=()=>cache.invalidateQueries({queryKey:['project-attachments',id]});const upload=useMutation({mutationFn:()=>projectService.upload(id,{file:file!,visitExternalId:visit,attachmentType:type||options.data!.attachmentTypes[0].value,caption:caption.trim(),isCover,occurredAtUtc:new Date().toISOString()}),onSuccess:async()=>{setUploadOpen(false);setFile(null);setCaption('');notify('Adjunto cargado correctamente.');await refresh()}}),remove=useMutation({mutationFn:(attachmentId:string)=>projectService.deleteAttachment(id,attachmentId),onSuccess:async()=>{setDeleting(null);await refresh()}}),cover=useMutation({mutationFn:(attachmentId:string)=>projectService.setCover(id,attachmentId),onSuccess:refresh});const files=useMemo(()=>(query.data||[]).filter(item=>filter==='all'||(filter==='photos'?item.contentType.startsWith('image/'):!item.contentType.startsWith('image/'))),[query.data,filter]);const submit=(event:FormEvent)=>{event.preventDefault();const error=validateAttachment(file,options.data);if(error)return setValidation(error);if(!visit)return setValidation('Selecciona la visita asociada.');setValidation(null);upload.mutate()};return <section className="project-attachments"><header><nav>{([['all','Todos'],['photos','Fotos'],['documents','Documentos']]as[FileFilter,string][]).map(([value,label])=><button className={filter===value?'active':''} key={value} onClick={()=>setFilter(value)}>{label}</button>)}</nav>{editable&&<button className="action-primary" onClick={()=>setUploadOpen(true)}><Upload/>Subir archivo</button>}</header><div className="project-gallery">{files.map(item=><article className={item.isCover?'cover':''} key={item.externalId}>{item.contentType.startsWith('image/')?<a href={item.downloadUrl} target="_blank" rel="noreferrer"><img src={item.downloadUrl} alt={item.caption||'Fotografía del proyecto'}/></a>:<a className="document-preview" href={item.downloadUrl} target="_blank" rel="noreferrer"><FileText/></a>}<div><strong>{item.caption||friendlyFileName(item.contentType)}</strong>{item.isCover&&<span>★ Portada</span>}<p>{item.attachmentType} · {formatDate(item.createdAtUtc)}</p><small>{formatBytes(item.sizeBytes)} · {item.uploadedByUserName}</small></div>{editable&&<footer>{!item.isCover&&<button onClick={()=>cover.mutate(item.externalId)}>Usar como portada</button>}<button className="danger-text" onClick={()=>setDeleting({id:item.externalId,name:item.caption||friendlyFileName(item.contentType)})}>Eliminar</button></footer>}</article>)}</div>{!query.isLoading&&!files.length&&<p className="activity-empty">No hay archivos para este filtro.</p>}{uploadOpen&&<div className="upload-drawer-backdrop"><aside className="upload-drawer"><button className="panel-close" onClick={()=>setUploadOpen(false)}><X/></button><h3>Subir archivo</h3><form onSubmit={submit}><label>Visita asociada<select required value={visit} onChange={event=>setVisit(event.target.value)}><option value="">Seleccionar visita</option>{visits.data?.map(item=><option key={item.externalId} value={item.externalId}>{formatDateTime(item.visitedAtUtc)} · {item.sellerName}</option>)}</select></label><label>Tipo<select value={type} onChange={event=>setType(event.target.value)}>{options.data?.attachmentTypes.map(item=><option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label>Descripción<input value={caption} maxLength={250} onChange={event=>setCaption(event.target.value)}/></label><label>Archivo<input required type="file" onChange={event=>{const selected=event.target.files?.[0]||null;setFile(selected);setValidation(validateAttachment(selected,options.data))}}/></label><label className="cover-check"><input type="checkbox" checked={isCover} onChange={event=>setIsCover(event.target.checked)}/> Usar como portada</label>{validation&&<p className="form-error">{validation}</p>}<button className="action-primary" disabled={upload.isPending}>{upload.isPending?'Subiendo…':'Subir archivo'}</button></form></aside></div>}<ConfirmDialog open={Boolean(deleting)} title="Eliminar adjunto" description={`Se eliminará ${deleting?.name||'este archivo'} de forma permanente.`} confirmLabel="Eliminar" busy={remove.isPending} onCancel={()=>setDeleting(null)} onConfirm={()=>deleting&&remove.mutate(deleting.id)}/></section>}

const friendlyFileName=(contentType:string)=>contentType.startsWith('image/')?'Fotografía':contentType.includes('pdf')?'Documento PDF':'Documento'
const formatDate=(value:string)=>new Intl.DateTimeFormat('es-BO',{dateStyle:'medium'}).format(new Date(value))
const formatDateTime=(value:string)=>new Intl.DateTimeFormat('es-BO',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value))
