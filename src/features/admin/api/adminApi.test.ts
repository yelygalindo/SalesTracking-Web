import MockAdapter from 'axios-mock-adapter'
import { beforeEach, describe, expect, it } from 'vitest'
import { apiClient, publicApiClient } from '@/lib/api/apiClient'
import { adminApi } from './adminApi'
describe('adminApi',()=>{const api=new MockAdapter(apiClient),publicApi=new MockAdapter(publicApiClient);beforeEach(()=>{api.reset();publicApi.reset()});it('crea una invitación con el contrato publicado',async()=>{api.onPost('/api/invitations').reply(201,{externalId:'inv-1',email:'ana@example.com'});await adminApi.invite({email:'ana@example.com',fullName:'Ana Pérez',roleCode:'seller'});expect(JSON.parse(api.history.post[0].data)).toEqual({email:'ana@example.com',fullName:'Ana Pérez',roleCode:'seller'})});it('consulta listado e historial administrativos',async()=>{api.onGet('/api/invitations').reply(200,[]);api.onGet('/api/invitations/manage/inv-1/history').reply(200,{invitation:{externalId:'inv-1'},events:[]});await adminApi.invitations();await adminApi.invitationHistory('inv-1');expect(api.history.get.map(x=>x.url)).toEqual(['/api/invitations','/api/invitations/manage/inv-1/history'])});it('cancela una invitación',async()=>{api.onPost('/api/invitations/inv-1/cancel').reply(200,{message:'Cancelada'});await adminApi.cancel('inv-1');expect(api.history.post[0].url).toBe('/api/invitations/inv-1/cancel')});it('acepta una invitación de manera pública',async()=>{publicApi.onPost('/api/invitations/accept').reply(200,{message:'Aceptada',externalUserId:'usr-1'});await adminApi.accept({token:'abc',password:'Secure123!',fullNameUser:'Ana'});expect(JSON.parse(publicApi.history.post[0].data)).toEqual({token:'abc',password:'Secure123!',fullNameUser:'Ana'})})})
