import{describe,expect,it}from'vitest';import{updateCustomerStatus,updateProjectProgress}from'./optimisticUpdates';describe('optimisticUpdates',()=>{it('actualiza inmediatamente el estado del cliente',()=>{const result=updateCustomerStatus({customers:[{externalId:'c1',status:'Inactivo'}]},'c1','Activo');expect(result?.customers[0].status).toBe('Activo')});it('actualiza inmediatamente el avance de obra',()=>{const result=updateProjectProgress({items:[{externalId:'p1',progressPercentage:20}]},'p1',65);expect(result?.items[0].progressPercentage).toBe(65)})})
