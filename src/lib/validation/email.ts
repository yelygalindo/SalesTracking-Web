export const emailPattern=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;export function validateOptionalEmail(value:string|null|undefined){const email=value?.trim();return!email||emailPattern.test(email)?null:'Ingresa un correo válido, por ejemplo nombre@empresa.com.'}export function validateRequiredEmail(value:string){return value.trim()&&emailPattern.test(value.trim())?null:'Ingresa un correo válido, por ejemplo nombre@empresa.com.'}
